class Sky extends Sprite {
    // constructor
    constructor(status, game) {
        super(status, game)
        this.setup()
    }

    setup() {
    }

    update() {
        super.update()
    }
}